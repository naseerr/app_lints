name: app_lints
description: Custom lint rules enforcing flutter_screenutil, color constants, text styles, localization, and safe GetX usage across Flutter apps.
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  analyzer: ^8.4.0
  analyzer_plugin: '>=0.11.0 <0.14.0'
  custom_lint_builder: ^0.8.1

dev_dependencies:
  custom_lint: ^0.8.1
  lints: ^4.0.0
  test: ^1.24.0
